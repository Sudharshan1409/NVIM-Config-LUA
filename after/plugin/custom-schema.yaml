$id: "https://example.com/custom-schema.yaml"
$schema: "http://json-schema.org/draft-07/schema#"

definitions:
  CustomTag:
    type: string

tags:
  - name: "!Ref"
    description: "CloudFormation Ref tag"
    schema: { $ref: "#/definitions/CustomTag" }
